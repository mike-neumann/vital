# vital-commands-processor

This module can be used to add the processor for the `vital-commands` module to your plugin.  
It will dynamically look for declared commands and register them to automatically generate your `plugin.yml` file.
