# vital-cloudnet4-driver

This submodule can be used to include a developer-friendly wrapper api over the native cloudnet4-driver apis.  
It will contain utility functions to easily interact with the cloudnet4-driver api, without having to manually interact with it yourself.
