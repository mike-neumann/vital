---
title: About the vital-tasks module.
description: This page explains what the `vital-tasks` module is and how you can use it to create repeatable tasks and countdowns using your server's scheduler.
navigation:
  icon: i-lucide-info
  title: About this module
---

# About

With this module, you can create repeatable tasks and countdown tasks that run directly on your server's scheduler (`Bukkit.getScheduler()`, `ProxyServer.getInstance().getScheduler()`, etc.).  
This allows you to directly call synchronous server APIs that MUST run on the server's main thread in a structured manner.  

Please refer to the respective documentation for both tasks on the left navigation.

## Platform compatability

| Platform   | Compatible | Notes                                                                                                   |
|:-----------|:-----------|:--------------------------------------------------------------------------------------------------------|
| Spigot     | ✅          | This platform is fully supported. Use platform-specific implementation subclasses (`SomeClass.Spigot`). |
| Paper      | ✅          | This platform is fully supported. Use platform-specific implementation subclasses (`SomeClass.Spigot`). |
| BungeeCord | ✅          | This platform is fully supported. Use platform-specific implementation subclasses (`SomeClass.Bungee`). |
