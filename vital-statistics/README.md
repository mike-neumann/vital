# vital-statistics

This submodule can be used to include Vital's statistics monitor system into your plugin.  
It contains a system, which monitors plugin and server-health and notifies the console when it detects any anomalies.
