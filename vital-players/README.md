# vital-players

This module can be used to add Vital's advanced player system to your plugin.  
It provides the means of managing custom player instances to store player-specific session data on it.  

When a player joins the server, Vital will automatically create an instance of your configured player class.  
This class can be used to store player-specific data directly on a class instance, that you can retrieve at any given time.  
