# vital-cloudnet4-bridge

This submodule can be used to include a developer-friendly wrapper api over the native cloudnet4-bridge apis.  
It will contain utility functions to easily interact with the cloudnet4-bridge api, without having to manually interact with it yourself.
