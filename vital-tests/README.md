# vital-tests

This submodule can be used to include Vital's testsuite into your plugin.  
It will contain some dependencies, which are required to run tests when building a plugin using Vital.  

This submodule should not be present in your plugin output.
